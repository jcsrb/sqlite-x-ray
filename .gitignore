node_modules/
dist/
*.local
.DS_Store

# Never commit databases dropped in here (they may be real/private data)
*.db
*.db3
*.sqlite
*.sqlite3
*.gz
# ...except the bundled synthetic demo, which ships with the app
!public/demo.sqlite
